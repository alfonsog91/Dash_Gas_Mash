- [x] Verify that the copilot-instructions.md file in the .github directory is created.

- [x] Clarify Project Requirements (static HTML/JS, Leaflet + OSM tiles, Overpass POIs, heuristic probability heatmap).

- [x] Scaffold the Project (manual static app scaffold; Node/Vite not available in PATH).

- [x] Customize the Project (implemented map, Overpass fetch, heatmap scoring, UI, and README).

- [x] Install Required Extensions (skipped; none required).

- [x] Compile the Project (skipped; no build step).

- [x] Create and Run Task (skipped; run via `python -m http.server`).

- [ ] Launch the Project (prompt user before starting a debug/launch workflow).

- [x] Ensure Documentation is Complete (README.md added; comments removed).

- Work through each checklist item systematically.
- Keep communication concise and focused.
- Follow development best practices.
